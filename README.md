# The Balloon Language

**This is a very experimental project.**

If you want to run the binary:

```sh
$ cargo run parse tests/001-single-simple-assignment.bl
$ cargo run run tests/001-single-simple-assignment.bl
```
